# 언제했지 앱 개발 작업 지시서

## 1. 프로젝트 개요

앱 이름은 **언제했지**이다.

이 앱은 사용자가 자주 하지는 않지만 가끔 꼭 해야 하는 생활 관리 일을 마지막으로 언제 했는지 기록하고, 권장 주기가 지나면 알려주는 로컬 기반 생활 주기 관리 앱이다.

예시 항목:

- 침구 세탁
- 칫솔 교체
- 에어컨 필터 청소
- 냉장고 청소
- 세탁조 청소
- 강아지 목욕
- 자동차 세차
- 정수기 필터 교체
- 스케일링
- 건강검진

앱의 핵심은 단순한 할 일 관리가 아니다.

이 앱은 사용자가 다음 질문을 할 때 바로 답을 주는 앱이어야 한다.

> "이거 마지막으로 언제 했지?"

따라서 UI와 기능은 날짜 자체보다 **마지막으로 한 지 며칠 지났는지**, **주기까지 얼마나 남았는지**, **얼마나 초과했는지**를 중심으로 설계한다.

---

## 2. 개발 목표

1차 MVP를 개발한다.

서버는 사용하지 않는다.  
회원가입도 사용하지 않는다.  
모든 데이터는 기기 로컬에 저장한다.

### 핵심 목표

- 사용자가 생활 관리 항목을 등록할 수 있어야 한다.
- 각 항목은 마지막 수행일과 권장 주기를 가진다.
- 앱은 마지막 수행일 기준으로 경과일, 남은일, 초과일을 계산해야 한다.
- 사용자가 항목을 완료하면 "오늘 했어요" 버튼으로 마지막 수행일이 오늘로 갱신되어야 한다.
- 항목별 수행 히스토리가 저장되어야 한다.
- 권장일이 도래하면 로컬 알림을 보낼 수 있어야 한다.
- 무료 사용자는 하단 배너 광고를 본다.
- Plus 사용자는 광고가 제거된다.

---

## 3. 기술 조건

우선 Android 앱으로 개발한다.

권장 기술:

- Language: Kotlin 또는 Java
- Architecture: MVVM 권장
- Local DB: Room
- Local Notification: WorkManager 또는 AlarmManager
- UI: Jetpack Compose 또는 XML View 중 프로젝트 상황에 맞게 선택
- 광고: Google AdMob Banner
- 인앱결제: Google Play Billing
- 서버/API 사용 금지
- 클라우드 동기화 없음
- 로그인 없음

기술 선택 시 과도한 추상화는 피한다.  
MVP를 빠르게 완성하는 것을 우선한다.

---

## 4. 앱 핵심 개념

이 앱은 다음 앱들과 다르다.

### 할 일 앱이 아님

할 일 앱은 앞으로 해야 할 일을 관리한다.  
언제했지는 마지막으로 한 지 얼마나 됐는지를 보여준다.

### 습관 앱이 아님

습관 앱은 매일 또는 자주 반복하는 행동을 체크한다.  
언제했지는 가끔 해야 하는 생활 관리 주기를 추적한다.

### 캘린더 앱이 아님

캘린더는 특정 날짜의 일정을 보여준다.  
언제했지는 "마지막으로 한 지 며칠째인지"를 보여준다.

---

## 5. 주요 기능

## 5.1 항목 등록

사용자는 관리할 항목을 등록할 수 있어야 한다.

필수 입력값:

- 항목명
- 카테고리
- 마지막 수행일
- 권장 주기

선택 입력값:

- 메모
- 알림 사용 여부
- 알림 시간
- 아이콘 또는 이모지

예시:

```text
항목명: 침구 세탁
카테고리: 집안관리
마지막 수행일: 2026-05-01
권장 주기: 14일
알림: 사용
메모: 이불, 베개커버 포함
```

검증 규칙:

- 항목명은 1~20자.
- 권장 주기는 1일 이상.
- 마지막 수행일은 오늘 또는 과거 날짜만 허용.

---

## 5.2 목록 화면 (홈)

앱의 첫 화면이자 가장 중요한 화면이다.
사용자가 앱을 열었을 때 "지금 뭐가 급하지?"에 즉시 답해야 한다.

### 카드에 표시할 정보

- 아이콘 또는 이모지
- 항목명
- 카테고리
- **핵심 지표 (가장 크게)**
  - 초과 상태: `+N일 초과`
  - 임박 상태: `D-N`
  - 여유 상태: `N일째` 또는 `D-N`
- 보조 정보: 마지막 수행일 (작게)

날짜 자체는 보조 정보다.
큰 숫자는 항상 경과/남은/초과를 보여준다.

### 정렬 (기본)

1. 초과 항목 (초과일 많은 순)
2. 임박 항목 (남은일 적은 순)
3. 여유 항목 (남은일 많은 순)

사용자가 다음 기준으로 변경 가능:

- 카테고리별
- 마지막 수행일 순
- 등록 순

### 상태 색상 규칙

| 상태 | 조건 | 색상 |
|---|---|---|
| 초과 | 경과일 > 권장 주기 | 빨강 |
| 임박 | 남은일 ≤ **임박 기준일** | 노랑 |
| 여유 | 남은일 > **임박 기준일** | 회색 또는 초록 |

**임박 기준일**은 사용자가 앱 설정에서 변경할 수 있다 (기본값 3일, 범위 1~14일).
8장 "앱 설정" 참조.

색상은 카드 배경 전체가 아니라 **핵심 숫자와 상태 뱃지에만** 적용한다.
배경 전체를 칠하면 시각적 피로가 크다.

### 빈 상태

항목이 0개일 때:

- 짧은 안내 문구
- "첫 항목 추가하기" 버튼
- 추천 항목 예시 3~5개 (탭 시 등록 화면으로 이동)

---

## 5.3 항목 상세 화면

목록에서 항목을 탭하면 진입한다.

### 표시 정보

- 상단 큰 영역: 핵심 지표를 매우 크게
  - `마지막으로 한 지 N일째`
  - `다음 권장일까지 D-N`
- 마지막 수행일
- 권장 주기
- 카테고리
- 메모
- **"오늘 했어요" 버튼** (가장 큰 액션 버튼)
- 수정 / 삭제 메뉴
- 수행 히스토리

### 수행 히스토리

날짜 역순으로 표시한다.

- 수행일
- 직전 수행과의 간격 (예: `이전보다 12일 후`)
- 메모 (선택)

히스토리 항목은 길게 눌러 삭제할 수 있다.

---

## 5.4 "오늘 했어요" 동작

이 앱에서 가장 자주 쓰는 액션이다.
한 번 누르면 끝나야 한다.

### 동작 순서

1. 마지막 수행일을 오늘로 갱신.
2. 수행 히스토리에 한 줄 추가.
3. 다음 권장일 재계산.
4. 토스트 또는 스낵바로 피드백
   - 예: `기록됨. 다음 권장일: 2026-05-21`

### 추가 옵션

기본은 "오늘"이지만 다른 날짜로도 기록 가능.

- 버튼 길게 누르기 → 날짜 선택
- 어제 / 그저께 / 직접 선택

---

## 5.5 알림

권장일이 도래하면 로컬 알림을 보낸다.

### 트리거 조건

- 항목별 알림 사용이 켜져 있고,
- 오늘이 다음 권장일과 같거나 그 이후이면,
- 사용자 지정 알림 시간에 1회 발송.

### 알림 내용

- 제목: `침구 세탁 할 때가 됐어요`
- 본문: `마지막으로 한 지 14일 지났어요`
- 액션 버튼: `오늘 했어요` / `내일 다시 알림`

### 미루기 (snooze)

`내일 다시 알림` 선택 시 다음 날 같은 시간에 재발송.

### 구현 방식

- 기본: WorkManager로 매일 1회 스케줄링 → 트리거 시 알림.
- 정확 시간 필요한 경우: AlarmManager + `setExactAndAllowWhileIdle`.

---

## 5.6 카테고리

기본 카테고리를 제공하고, 사용자가 추가할 수 있다.

### 기본 카테고리

- 집안관리
- 개인위생
- 차량
- 반려동물
- 건강
- 기타

### 카테고리 관리

- 추가 / 이름 수정 / 삭제 가능.
- 삭제 시 해당 카테고리 항목은 자동으로 "기타"로 이동.

---

## 5.7 광고 및 Plus 결제

### 무료 사용자

- 홈, 상세 화면 하단에 AdMob 배너 노출.
- 등록 / 수정 / 알림 설정 화면에는 광고를 띄우지 않는다.

### Plus 사용자

- 광고 제거.
- 그 외 기능은 무료와 동일 (MVP에서 광고 제거가 유일한 차이).

### 결제

- Google Play Billing 사용.
- MVP는 상품을 단순화: **"Plus 평생 이용권"** 1개로 시작.
- 구독형은 정착 후 도입 검토.

---

## 6. 데이터 모델

Room으로 구현한다.

### Item

| 필드 | 타입 | 설명 |
|---|---|---|
| id | Long | PK, autoGenerate |
| name | String | 항목명 |
| categoryId | Long | FK |
| lastDoneDate | LocalDate | 마지막 수행일 |
| intervalDays | Int | 권장 주기 (일) |
| icon | String? | 이모지 또는 리소스 키 |
| memo | String? | 메모 |
| notifyEnabled | Boolean | 알림 사용 여부 |
| notifyTime | LocalTime? | 알림 시간 |
| createdAt | LocalDateTime | 등록 시각 |

### History

| 필드 | 타입 | 설명 |
|---|---|---|
| id | Long | PK |
| itemId | Long | FK |
| doneDate | LocalDate | 수행일 |
| memo | String? | 메모 |

### Category

| 필드 | 타입 | 설명 |
|---|---|---|
| id | Long | PK |
| name | String | 카테고리명 |
| isDefault | Boolean | 기본 제공 여부 |

---

## 7. UI/UX 가이드라인

### 핵심 원칙

- 날짜보다 **경과일 / 남은일 / 초과일**을 항상 더 크게.
- "오늘 했어요" 버튼은 한 손으로 닿는 위치에.
- 색상은 의미 있는 곳에만 (초과 / 임박 표시).

### 타이포그래피

- 핵심 숫자 (경과일 등): 굵고 매우 크게 (Display 사이즈).
- 보조 정보 (날짜, 카테고리): 작게.

### 다크 모드

지원한다. 시스템 설정을 따른다.

---

## 8. 앱 설정

사용자가 변경 가능한 전역 설정.

| 항목 | 기본값 | 범위 / 옵션 |
|---|---|---|
| 임박 기준일 | 3일 | 1~14일 |
| 기본 알림 시간 | 09:00 | 시간 선택 |
| 다크 모드 | 시스템 설정 따름 | 시스템 / 라이트 / 다크 |
| 시작 화면 정렬 | 상태 우선 | 상태 / 카테고리 / 마지막 수행일 / 등록 순 |

설정은 SharedPreferences 또는 DataStore에 저장한다.

---

## 9. MVP 제외 범위

다음은 MVP에서 다루지 않는다.

- iOS 앱
- 클라우드 동기화 / 백업
- 위젯, 잠금화면 위젯
- 통계 / 리포트
- 가족 / 공유 기능
- 항목 템플릿 마켓
- 다국어 (한국어만)

1차 출시 후 사용자 반응을 보고 결정한다.

---

## 10. 다음 TASK (출시 마무리 + 후속)

MVP 코어 구현 완료 후 출시 전/후 진행할 작업.

### 10.1 출시 전 (Release 준비)

- **Release 빌드 설정**
  - `signingConfig` 정의 + 키스토어 생성 (`*.keystore` 파일은 git 관리 외 + `local.properties` 또는 환경변수로 비밀번호)
  - `buildTypes.release` `isMinifyEnabled = true`, `proguardFiles` 검토
  - R8 룰 점검 (Room/Compose/AdMob/WorkManager 자동 keep 확인)
- **AdMob 실 단위 ID 교체**
  - AdMob 콘솔에서 앱 등록 → 광고 단위 ID 발급
  - `BuildConfig` 분기로 debug = test ID, release = real ID
  - `AndroidManifest`의 App ID `meta-data`도 release용으로 분기
- **Google Play Console 등록 준비**
  - 개인정보처리방침 URL (필수: AdMob/POST_NOTIFICATIONS 권한 사용)
  - 스토어 리스팅: 스크린샷, 앱 설명, 짧은 소개, 카테고리, 콘텐츠 등급
  - 데이터 안전 섹션 (수집 데이터 없음, AdMob 추적 ID는 제외 신청 가능)
- **앱 아이콘 정식화**
  - 현재 `drawable/ic_launcher.xml` 임시 vector
  - Asset Studio 또는 디자인 도구로 adaptive icon (foreground + background) 제작
  - mipmap-* PNG/webp 다해상도 생성

### 10.2 출시 후 (Phase 8 미완 + 폴리시)

- **Google Play Billing — Plus 평생 이용권**
  - BillingClient 래퍼, `PlusRepository` 실 backing 교체
  - 구매 복원 흐름, Pending 상태 처리
  - 설정의 "Plus 구매하기" 버튼 와이어링
- **AdView lifecycle 정리**
  - `DisposableEffect`로 `pause/resume/destroy` 호출 (백그라운드 광고 갱신 방지)
- **빈 상태 추천 항목 예시 (TASK.md 5.2)**
  - "첫 항목 추가하기" 안내에 추천 항목 카드 3~5개 (탭 → 등록 화면 프리필)
- **수정 시 lastDoneDate ↔ history 일관성**
  - 마지막 history 항목 같이 갱신 (또는 사용자에게 옵션 제시)

### 10.3 추후 기능 (사용자 반응 보고)

- **위젯** (홈 화면 위젯) — 임박/초과 항목 한눈에
- **통계** — 카테고리별 평균 주기, 가장 오래 지난 항목 등
- **항목별 알림 시간 override** — TASK.md 5.5 명시. 현재 전역 시간만
- **알림 snooze ("내일 다시 알림")** — TASK.md 5.5 명시
- **백업/복구 (로컬 export)** — JSON/CSV 내보내기, 가져오기
- **클라우드 동기화** — Google Drive 또는 자체 백엔드
- **카테고리 관리 UI** — 추가/수정/삭제 (현재 기본 6종 시드만)
- **다국어** — 영어 + 일본어 추가
